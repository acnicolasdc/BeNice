import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    table: {
      position: 'fixed',
      right: 10,
      top: 92,
      maxWidth: 250,
      maxHeight: 500,
    },
  });

export default useStyles;